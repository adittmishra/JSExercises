// Convert the below functions into arrow functions.

const greetings = () => {
  return "Hello World";
};

const isEven = (num) => {
  if (num % 2 == 0) {
    return true;
  } else {
    return false;
  }
};
